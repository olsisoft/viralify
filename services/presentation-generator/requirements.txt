# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Pydantic for data models
pydantic==2.5.3
pydantic-settings==2.1.0

# HTTP client
httpx==0.26.0
aiohttp==3.9.1

# OpenAI for GPT-4 planning
openai==1.12.0

# Image generation
Pillow==10.2.0
Pygments==2.18.0

# Cloudinary for image storage
cloudinary==1.38.0

# LangGraph for workflow orchestration
langgraph>=0.2.0
langchain-core>=0.2.27,<0.3

# Redis for shared job storage
redis>=5.0.0

# System monitoring for FFmpeg resource management
psutil>=5.9.0

# Utils
python-dotenv==1.0.0
uuid==1.30

# Visual Generator dependencies (Phase 6)
matplotlib>=3.8.0
numpy>=1.24.0
diagrams>=0.23.4  # Professional architecture diagrams with AWS/Azure/GCP icons

# SSVS Embedding engines (optional - TF-IDF fallback if not installed)
# Set SSVS_EMBEDDING_BACKEND=minilm|bge-m3|tfidf|auto
sentence-transformers>=2.2.0  # MiniLM and BGE-M3 support
torch>=2.0.0  # Required for sentence-transformers (CPU version)
